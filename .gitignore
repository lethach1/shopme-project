# Thư mục build và target (Java, Maven, Gradle)
target/
build/

# Thư mục của IDE (IntelliJ IDEA, Eclipse, VSCode)
.idea/
.vscode/

# Tệp cấu hình nhạy cảm
*.yml
*.env

# Các tệp log, tệp tạm thời
*.log
*.tmp

# Các tệp hệ thống (macOS, Windows)
.DS_Store
Thumbs.db

# Các tệp nhạy cảm khác cần tránh, ví dụ API keys, credentials, hoặc các tệp có dữ liệu bảo mật
application.properties
application.yml

# Các thư mục cài đặt phụ thuộc (nếu có)
node_modules/
*.jar

# Tệp cấu hình Maven (nếu có)
*.iml
*.classpath
*.project
